for aliasfile in ~/.config/fish/aliases/*.fish
    source $aliasfile
end

# The next line updates PATH for Netlify's Git Credential Helper.
# test -f '~/Library/Preferences/netlify/helper/path.fish.inc' && source '~/Library/Preferences/netlify/helper/path.fish.inc'

